#include <stdio.h>
#include <malloc.h>
#include <string.h>
int main ()
{
    char str[] = "simran";
    char *ptr = str;
    int count = 0;
    while(*ptr)
    {
        printf("%c\n", *ptr);
        count++;
        ptr++;
    }

    printf("\n%d\n", count);
}   
