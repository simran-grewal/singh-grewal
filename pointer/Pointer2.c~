#include <stdio.h>
#include <malloc.h>
#include <string.h>
int main ()
{
    char str[] = "simran";
    char *ptr = &str[0];
    int count = 0;
    char *s = ptr;
    while(*ptr)
    {
        *ptr += 1;
        printf("%c\n", *ptr);
        ptr++;
    }

    printf("\n\n\n");
    while(*s)
    {
        printf("%c\n", *s);
       
        s++;
    }
}   
