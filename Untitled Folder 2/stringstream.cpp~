#include<bits/stdc++.h>
using namespace std;
int main()
{
    string str;
    string temp;
    string empp;
    getline(cin,str);
    stringstream obj(str);
    while(obj>>temp)
    {
        transform(temp.begin(),temp.end(),temp.begin(),::tolower);
        cout<<temp<<" ";
    }
}
