#include <stdio.h>

int main ()
{
    // declaring 2D array in c ------- const char *options[2] = { "test1", "test2" };

      /*
  char buffer [50];
  int n, a=5, b=3;
  n=sprintf (buffer, "%d plus %d is %d", a, b, a+b);
  printf ("[%s] is a string %d chars long\n",buffer,n);
      */

    /*
     char str[20];
     int hours = 16;
     int l = sprintf(str, "%d:%02dsimran", hours, 5);      // l is length of string && // all the content will be put in the str;
     printf("%s\n%d", str, l);
        */
    /*
    char str[50];
    int l = sprintf(str, "my name is simran");
    printf("%s \n %d", str, l);
    */

        /*
    char str[50];
    char str1[50];
    int l = sprintf(str1,gets(str));
    printf("%s\n", str1);
    printf("%s\n", str);
    printf("%d", l);
        */
     return 0;
}
