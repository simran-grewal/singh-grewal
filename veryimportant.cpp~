#include<bits/stdc++.h>
using namespace std;
int main()
{
    int k=0;
    char str[5][40];
    for(int i=0;i<5;i++)
        gets(str[i]);
    char str1[60];
    char *arr;
    arr=(char *)malloc(50*sizeof(char));
    for(int i=0;i<5;i++)
    {
       arr=str[i];
        for(int j=0;j<3;j++)
        {
            if(isalpha(arr[j]))
            {
                str1[k++]=arr[j];
            }
        }
    }

    str1[k++]='\0';
    cout<<str1<<endl;
    
}
